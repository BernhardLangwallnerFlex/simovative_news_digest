# Pin to the Playwright Docker base image version (mcr.microsoft.com/playwright/python:v1.58.0-noble).
# An unpinned version lets a rebuild pull a newer Playwright whose bundled chromium
# build is absent from the base image -> "Executable doesn't exist" at launch.
playwright==1.58.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.10
requests>=2.31.0
openai>=1.30.0
python-dotenv>=1.0.0
readability-lxml>=0.8.1
sentence-transformers>=2.2.0
azure-storage-blob>=12.19.0
pytest>=8.0.0
